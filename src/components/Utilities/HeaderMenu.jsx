import React from "react";

const HeaderMenu = ({ title }) => {
  return (
    <div className="text-white text-center p-4 font-semibold text-4xl">
      {title}
    </div>
  );
};

export default HeaderMenu;
